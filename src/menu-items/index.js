// project import
import auth from './auth';
import platform from './platform';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [platform, auth],
};

export default menuItems;
